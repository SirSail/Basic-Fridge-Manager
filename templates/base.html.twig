<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
            <style>
                /* Ogólne style dla strony */
                body {
                    font-family: 'Segoe UI Emoji', 'Apple Color Emoji', 'Noto Color Emoji', sans-serif;
                    margin: 0;
                    padding: 0;
                    display: flex;
                }

                /* Styl dla panelu nawigacyjnego */
                .sidebar {
                    position: fixed;
                    top: 0;
                    left: 0;
                    width: 200px;
                    height: 100%;
                    background-color: #f0f0f0;
                    padding: 1em;
                    box-shadow: 2px 0 5px rgba(0,0,0,0.1);
                }

                .sidebar a {
                    display: block;
                    margin: 1em 0;
                    font-size: 1em;
                    text-decoration: none;
                    color: #007BFF;
                }

                .sidebar a:hover {
                    text-decoration: underline;
                }

                /* Styl dla głównej zawartości */
                .main-content {
                    margin-left: 220px; /* Dodajemy margines dla głównej zawartości, aby nie była ukryta za panelem nawigacyjnym */
                    padding: 1em;
                    flex-grow: 1;
                }

                /* Styl dla alertów */
                .alert {
                    padding: 0.75em 1em;
                    margin-bottom: 1em;
                    border: 1px solid transparent;
                    border-radius: 0.25em;
                }

                .alert-danger {
                    color: #721c24;
                    background-color: #f8d7da;
                    border-color: #f5c6cb;
                }
            </style>
        {% endblock %}
    </head>
    <body>
        <div class="sidebar">
        <a href="{{ path('app_conference') }}">&#127968; Strona Główna</a> <!-- Encja emoji 🏠 -->
             <a href="{{ path('app_fridge_list') }}">&#128295; Zarządzaj lodówkami</a> <!-- Encja emoji 🛠 -->
    <a href="{{ path('app_item_list') }}">&#128221; Zarządzaj przedmiotami</a> <!-- Encja emoji 📝 -->
    <a href="{{ path('app_user_list2') }}">&#128100; Zarządzaj użytkownikami</a> <!-- Encja emoji 👤 -->
    <a href="{{ path('app_category_list') }}">&#127991; Zarządzaj kategoriami</a> <!-- Encja emoji 🏷 -->
    <a href="{{ path('app_fridge_item_list') }}">&#129529; Zawartość lodówki</a> <!-- Encja emoji 🧺 -->
    <form method="post" action="{{ path('security_logout') }}">
        <button type="submit" class="btn btn-danger">Wyloguj</button>
    </form>
        </div>

        <div class="main-content">
            {% for message in app.flashes('error') %}
                <div class="alert alert-danger">
                    {{ message }}
                </div>
            {% endfor %}
            
            {% block body %}{% endblock %}
        </div>

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </body>
</html>
