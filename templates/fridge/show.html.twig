{% extends 'base.html.twig' %}

{% block title %}Fridge Details{% endblock %}

{% block body %}
<style>
    .main-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .main-wrapper h1 { margin-bottom: 1em; }
</style>

<div class="main-wrapper">
    <h1>{{ fridge.name }}</h1>

    <p><strong>Location:</strong> {{ fridge.location }}</p>

    <a href="{{ path('app_fridge_list') }}">Back to list</a>
</div>
{% endblock %}
